namespace FlowForge.App.ViewModels;

/// <summary>
/// 节点端口方向。
/// </summary>
public enum PortDirection
{
    /// <summary>
    /// 输入端口，位于节点左侧。
    /// </summary>
    Input,

    /// <summary>
    /// 输出端口，位于节点右侧。
    /// </summary>
    Output,
}
